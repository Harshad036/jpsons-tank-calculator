/** Resolve a public-folder asset path for GitHub Pages subdirectory deploys. */
export function assetUrl(path: string): string {
  const base = import.meta.env.BASE_URL;
  const clean = path.replace(/^\//, '');
  return `${base}${clean}`;
}
